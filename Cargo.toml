[workspace]
resolver = "2"
members = [
    "crates/rpytest",
    "crates/rpytest-core",
    "crates/rpytest-ipc",
    "crates/rpytest-daemon",
]

[workspace.package]
version = "0.1.1"
edition = "2021"
rust-version = "1.75"
license = "MIT OR Apache-2.0"
repository = "https://github.com/neul-labs/rpytest"
documentation = "https://docs.neullabs.com/rpytest"
homepage = "https://github.com/neul-labs/rpytest"
authors = ["neul-labs"]

[workspace.dependencies]
# CLI
clap = { version = "4.5", features = ["derive", "env", "string"] }

# Async
tokio = { version = "1.40", features = ["rt-multi-thread", "net", "sync", "time", "fs", "signal", "io-util"] }

# IPC
nng = "1.0"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rmp-serde = "1.3"

# Storage (behind abstraction)
sled = "0.34"

# Config parsing
toml = "0.8"
configparser = "3.1"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Terminal
console = "0.15"

# File watching
notify = "7.0"
notify-debouncer-mini = "0.5"

# Process utilities
which = "7.0"
libc = "0.2"

# Regex
regex-lite = "0.1"

# Internal crates
rpytest-core = { version = "0.1.1", path = "crates/rpytest-core" }
rpytest-ipc = { version = "0.1.1", path = "crates/rpytest-ipc" }

[workspace.dependencies.assert_cmd]
version = "2.0"

[workspace.dependencies.predicates]
version = "3.1"

[workspace.dependencies.tempfile]
version = "3.13"

# Optimized release profile
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"

# Fast release build (for CI or faster iteration)
[profile.release-fast]
inherits = "release"
lto = "thin"
codegen-units = 4
